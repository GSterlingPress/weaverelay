const clean=(v,n=160)=>String(v??'').replace(/[\r\n\t]+/g,' ').trim().slice(0,n);
const SAFE_ACTIONS=new Set(['navigate','assert-visible','click-link','click-control','wait-for-url']);
const BLOCKED_HINTS=/(buy|purchase|pay|checkout|order|delete|remove|terminate|cancel|publish|deploy|send|invite|transfer|withdraw|refund|charge|subscribe|sign.?up|create.?account|accept|agree)/i;
const SAFE_METHODS=new Set(['GET','HEAD']);
export function normalizeJourney(input={}){const steps=Array.isArray(input.steps)?input.steps:[];return{id:clean(input.id,80)||null,name:clean(input.name,100)||'Synthetic journey',startPath:clean(input.startPath,300)||'/',steps:steps.slice(0,20).map((s,i)=>({index:i+1,action:clean(s?.action,40),selector:clean(s?.selector,240)||null,path:clean(s?.path,300)||null,label:clean(s?.label,100)||null,method:clean(s?.method||'GET',12).toUpperCase()}))}}
export function reviewSyntheticJourney(input={}){const journey=normalizeJourney(input),reasons=[];if(!journey.id)reasons.push('Journey must have a stable id.');if(!journey.steps.length)reasons.push('Journey must contain at least one step.');for(const step of journey.steps){if(!SAFE_ACTIONS.has(step.action))reasons.push(`Step ${step.index} uses blocked action ${step.action||'unknown'}.`);if(!SAFE_METHODS.has(step.method))reasons.push(`Step ${step.index} would use a mutating HTTP method.`);const text=[step.label,step.selector,step.path].filter(Boolean).join(' ');if(BLOCKED_HINTS.test(text))reasons.push(`Step ${step.index} looks financially, destructively, legally, or account-state sensitive.`)}return{approved:reasons.length===0,mode:'read-only-browser',requiresExplicitApproval:true,allowsFormSubmission:false,allowsTyping:false,allowsMutationRequests:false,allowsPurchases:false,allowsDestructiveActions:false,reasons,journey}}
export function buildSyntheticEvidence(result={}){const status=result.status==='PASS'?'PASS':result.status==='FAIL'?'FAIL':'WARN';const step=result.failedStep||result.lastStep||null;return{id:'website.synthetic-journey',label:'Approved synthetic journey',status,detail:status==='PASS'?`Synthetic journey ${clean(result.name,100)||'approved journey'} completed without a proven browser failure.`:status==='FAIL'?`${clean(result.name,100)||'Approved journey'} failed${step?.label?` at ${clean(step.label,100)}`:''}${result.reason?`: ${clean(result.reason,240)}`:''}.`:'No recent approved synthetic journey result is available.',evidence:{source:'weaverelay-synthetic-browser',journeyId:clean(result.journeyId,80)||null,failedStep:step?{index:Number(step.index)||null,action:clean(step.action,40)||null,label:clean(step.label,100)||null}:null,customerContentRetained:false,formValuesRetained:false,responseBodiesRetained:false,cookiesRetained:false,mutationRequestsAllowed:false}}}
