const safeUrl=value=>{try{const u=new URL(String(value||''));return /^https?:$/.test(u.protocol)?u.toString():null}catch{return null}};
export function augmentSyntheticDiagnosis(diagnosis={},snapshot={}){const check=(snapshot.checks||[]).find(c=>c.id==='website.synthetic-journey');if(!check||check.status!=='FAIL')return diagnosis;const e=check.evidence||{},open=safeUrl(e.url),finding={id:'website-synthetic-journey-failed',severity:'critical',title:e.journeyName?`${e.journeyName} failed in a guarded browser replay`:'An approved browser journey is failing',explanation:check.detail,evidence:['website.synthetic-journey'],actions:['Open the exact failing page when available and correlate the failed browser step with source, deployment, runtime, and provider evidence.','Do not broaden the journey or enable a mutating action just to make the test pass.'],provider:null,repair:{supported:false,approvalRequired:true,label:'SHOW ME HOW'},openProvider:open?{label:'Open exact journey failure',url:open,depth:'exact-failure'}:null};const existing=(diagnosis.findings||[]).findIndex(f=>f.id===finding.id);if(existing>=0)diagnosis.findings[existing]=finding;else diagnosis.findings=[finding,...(diagnosis.findings||[])];diagnosis.status='broken';diagnosis.headline=finding.title;diagnosis.safeRepairs=[{finding:finding.id,label:'SHOW ME HOW',supported:false,approvalRequired:true,type:null,provider:null,openProvider:finding.openProvider},...(diagnosis.safeRepairs||[]).filter(r=>r.finding!==finding.id)];return diagnosis}
