import{requireUser}from'./_auth.mjs';import{requireWorkspace,readConnection,deleteConnection,deleteSecret,writeWorkspace}from'./_workspace-store.mjs';import{json,safeError}from'./_http.mjs';
export default async request=>{if(request.method!=='POST')return json(405,{error:'Method not allowed.'});try{const user=await requireUser(request),body=await request.json(),workspace=await requireWorkspace(user.id,body.workspaceId),provider=String(body.provider||''),connection=await readConnection(workspace.id,provider).catch(()=>null);if(connection?.id)await deleteSecret(connection.id);await deleteConnection(workspace.id,provider);workspace.providers=(workspace.providers||[]).map(p=>p.id===provider?{...p,status:'not_connected',detail:'Disconnected from WeaveRelay.',checkedAt:new Date().toISOString()}:p);workspace.updatedAt=new Date().toISOString();await writeWorkspace(workspace);return json(200,{ok:true,provider,status:'not_connected'});}catch(error){return safeError(error)}};
