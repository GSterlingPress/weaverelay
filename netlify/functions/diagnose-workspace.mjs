import{requireUser}from'./_auth.mjs';import{requireWorkspace,readConnection,readSecret,writeConnection,writeWorkspace}from'./_workspace-store.mjs';import{decryptSecret}from'./_vault.mjs';import{diagnoseSnapshot,sanitizeSnapshot}from'./_diagnose.mjs';import{probeCredential,checkForProvider}from'./_provider-probes.mjs';import{json,safeError}from'./_http.mjs';
const upsert=(checks,live)=>{const ix=checks.findIndex(c=>c.id===live.id);if(ix>=0)checks[ix]=live;else checks.push(live)};
export default async request=>{if(request.method!=='POST')return json(405,{error:'Method not allowed.'});try{const user=await requireUser(request),body=await request.json(),workspace=await requireWorkspace(user.id,body.workspaceId),seed=workspace.seedSnapshot||{product:workspace.name,topology:workspace.stackMap||{},checks:[]},checks=[...(seed.checks||[])],now=new Date().toISOString();for(const provider of ['github','netlify','railway','supabase','stripe']){const c=await readConnection(workspace.id,provider).catch(()=>null);if(!c?.id||c.status==='revoked')continue;try{const secret=decryptSecret(await readSecret(c.id));let live;if(provider==='github'){const r=await fetch('https://api.github.com/user',{headers:{authorization:`Bearer ${secret.accessToken}`,accept:'application/vnd.github+json','user-agent':'weaverelay'}});await r.text();live={id:'github.live',label:'GitHub',status:r.ok?'PASS':'FAIL',detail:r.ok?'GitHub answered a live read-only account probe.':`GitHub returned HTTP ${r.status}.`,evidence:{source:'weaverelay-live-oauth',...(r.ok?{}:{httpStatus:r.status})}};}else live=checkForProvider(provider,await probeCredential(provider,secret.accessToken));upsert(checks,live);c.lastCheckedAt=now;c.status=live.status==='PASS'?'connected':'error';c.updatedAt=now;await writeConnection(workspace.id,provider,c);}catch(error){const ids={github:'github.live',netlify:'netlify.account',railway:'railway.runtime',supabase:'supabase.live',stripe:'stripe.live'};upsert(checks,{id:ids[provider],label:provider[0].toUpperCase()+provider.slice(1),status:'WARN',detail:`Live ${provider} probe could not complete.`,evidence:{source:'weaverelay-live'}})}}const snapshot=sanitizeSnapshot({...seed,product:workspace.name,generatedAt:now,checks}),diagnosis=diagnoseSnapshot(snapshot);workspace.diagnosis=diagnosis;workspace.lastDiagnosticSnapshot=snapshot;workspace.updatedAt=now;workspace.status=diagnosis.status==='healthy'?'ready':'needs_action';await writeWorkspace(workspace);return json(200,{ok:true,workspaceId:workspace.id,diagnosis,checks:snapshot.checks,stackMap:workspace.stackMap});}catch(error){return safeError(error)}};
