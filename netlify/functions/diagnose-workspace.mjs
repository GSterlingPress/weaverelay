import{requireUser}from'./_auth.mjs';
import{requireWorkspace,readConnection,readSecret,writeConnection,writeWorkspace}from'./_workspace-store.mjs';
import{decryptSecret}from'./_vault.mjs';
import{diagnoseSnapshot,sanitizeSnapshot}from'./_diagnose.mjs';
import{probeCredential,checkForProvider}from'./_provider-probes.mjs';
import{buildCrossSystemEvidence}from'./_cross-system.mjs';
import{buildEnvironmentDeploymentEvidence}from'./_environment-deployment.mjs';
import{buildRuntimePaymentsEvidence}from'./_runtime-payments.mjs';
import{buildRunPodComfyUIEvidence}from'./_runpod-comfyui-proof.mjs';
import{buildRunPodAppRelationshipEvidence}from'./_runpod-app-relationship.mjs';
import{verifyRailwayRepairRuntime}from'./_railway-redeploy-repair.mjs';
import{verifyBackendSupabaseDependency}from'./_backend-dependency-proof.mjs';
import{inspectStripeWebhookRepair}from'./_stripe-webhook-repair.mjs';
import{verifyStripeWebhookDelivery}from'./_stripe-delivery-proof.mjs';
import{diagnoseStripeHandlerFailure}from'./_stripe-handler-diagnosis.mjs';
import{json,safeError}from'./_http.mjs';
const upsert=(checks,live)=>{const ix=checks.findIndex(c=>c.id===live.id);if(ix>=0)checks[ix]=live;else checks.push(live)};
const providerIds={github:'github.live',netlify:'netlify.account',railway:'railway.runtime',supabase:'supabase.live',stripe:'stripe.live',runpod:'runpod.live'};
const checkById=(checks,id)=>checks.find(c=>c.id===id);
const HANDLER_ACTIONS={'signature-configuration-missing':['Add the missing Stripe webhook-signature configuration in Railway, using the signing secret for this exact Stripe endpoint. Do not change the webhook URL again.'],'route-missing':['Restore or correct the webhook route in the Railway application so the exact Stripe path exists and accepts POST requests.'],'redirect':['Change the Stripe webhook destination to the final non-redirecting HTTPS route; Stripe treats redirects as failed deliveries.'],'access-blocked':['Remove login, auth middleware, IP blocking, or gateway restrictions from the Stripe webhook route while keeping Stripe signature verification in the handler.'],'handler-or-runtime-error':['Inspect the Railway logs for this webhook route and repair the application error producing the 5xx response.'],'timeout-or-network':['Make the handler return a 2xx response quickly and move slow work behind the acknowledgement; also verify Railway runtime/network availability.'],'network-unreachable':['Restore public reachability of the Railway webhook route before changing any Stripe configuration.'],'post-route-present':['The POST route appears to exist. Inspect Stripe delivery details and Railway logs for signature verification or application-processing errors.'],'request-validation-or-signature':['The route is reachable and rejects an unsigned probe, but a bad signing secret is not proven. Check Stripe delivery response details and the Railway webhook-signature configuration before changing code.'],'handler-processing-unclassified':['Open the failed Stripe delivery attempt and correlate it with Railway logs. The URL is correct; identify the exact handler response before making another change.']};
function sharpenStripeHandlerFinding(diagnosis,snapshot){const check=checkById(snapshot.checks,'repair.stripe-handler-failure');if(!check)return;const classification=check.evidence?.classification||check.evidence?.handlerFailureClass,existing=diagnosis.findings?.find(f=>f.id==='stripe-webhook-handler-failing');const titles={'signature-configuration-missing':'Stripe webhook signing configuration is missing','route-missing':'The Stripe webhook route is missing','redirect':'The Stripe webhook route redirects','access-blocked':'The Stripe webhook route is access-blocked','handler-or-runtime-error':'The Stripe webhook handler is returning a server error','timeout-or-network':'The Stripe webhook handler is timing out','network-unreachable':'The Stripe webhook handler is unreachable','post-route-present':'The Stripe POST route exists, but delivery still fails','request-validation-or-signature':'Stripe webhook request validation needs investigation','handler-processing-unclassified':'Stripe reaches the handler boundary, but processing still fails'};if(existing){existing.title=titles[classification]||existing.title;existing.explanation=check.detail;existing.evidence=[...new Set([...(existing.evidence||[]),'repair.stripe-handler-failure'])];existing.actions=HANDLER_ACTIONS[classification]||existing.actions;if(classification==='signature-configuration-missing'){existing.provider='railway';existing.openProvider={label:'Open Railway',url:'https://railway.com/dashboard'};existing.repair={supported:true,approvalRequired:true,type:'stripe-handler-secret',provider:'railway',label:'ADD WEBHOOK SECRET'}}else{existing.provider='stripe';existing.repair={supported:false,approvalRequired:true,label:'Guided handler repair'}}}}
function addHandlerRedeployAction(diagnosis,snapshot){const check=checkById(snapshot.checks,'repair.stripe-handler-runtime');if(!check||check.status==='PASS')return;const finding={id:'stripe-handler-redeploy',severity:'high',title:check.status==='FAIL'?'The repaired Stripe handler did not redeploy cleanly':'Railway must redeploy the repaired Stripe handler',explanation:check.detail,evidence:['repair.stripe-handler-runtime'],actions:[check.status==='FAIL'?'Open Railway and inspect the failed deployment before another change.':'Approve one redeploy of the already-proven Railway service. WeaveRelay will verify the runtime, then wait for a real Stripe event.'],provider:'railway',repair:check.status==='WARN'&&check.evidence?.redeployRequired===true?{supported:true,approvalRequired:true,type:'railway-redeploy',provider:'railway',label:'REDEPLOY & VERIFY'}:{supported:false,approvalRequired:true,label:'Open Railway'},openProvider:{label:'Open Railway',url:'https://railway.com/dashboard'}};diagnosis.findings=[finding,...(diagnosis.findings||[]).filter(f=>f.id!==finding.id)];diagnosis.safeRepairs=[{finding:finding.id,label:finding.repair.label,supported:Boolean(finding.repair.supported),approvalRequired:true,type:finding.repair.type||null,provider:'railway',openProvider:finding.openProvider},...(diagnosis.safeRepairs||[]).filter(r=>r.finding!==finding.id)];diagnosis.headline=finding.title;diagnosis.status='broken'}
export default async request=>{if(request.method!=='POST')return json(405,{error:'Method not allowed.'});try{const user=await requireUser(request),body=await request.json(),workspace=await requireWorkspace(user.id,body.workspaceId);const seed=workspace.seedSnapshot||{product:workspace.name,topology:workspace.stackMap||{},checks:[]},checks=[...(seed.checks||[])],secrets={},now=new Date().toISOString();
for(const provider of ['github','netlify','railway','supabase','stripe','runpod']){const c=await readConnection(workspace.id,provider).catch(()=>null);if(!c?.id||c.status==='revoked')continue;try{const secret=decryptSecret(await readSecret(c.id));secrets[provider]=secret.accessToken;let live;if(provider==='github'){const r=await fetch('https://api.github.com/user',{headers:{authorization:`Bearer ${secret.accessToken}`,accept:'application/vnd.github+json','user-agent':'weaverelay'}});await r.text();live={id:'github.live',label:'GitHub',status:r.ok?'PASS':'FAIL',detail:r.ok?'GitHub answered a live read-only account probe.':`GitHub returned HTTP ${r.status}.`,evidence:{source:'weaverelay-live-oauth',...(r.ok?{}:{httpStatus:r.status})}}}else live=checkForProvider(provider,await probeCredential(provider,secret.accessToken));upsert(checks,live);c.lastCheckedAt=now;c.status=live.status==='PASS'?'connected':'error';c.updatedAt=now;await writeConnection(workspace.id,provider,c)}catch{upsert(checks,{id:providerIds[provider],label:provider[0].toUpperCase()+provider.slice(1),status:'WARN',detail:`Live ${provider} probe could not complete.`,evidence:{source:'weaverelay-live'}})}}
let topology=workspace.stackMap||seed.topology||{};try{const cross=await buildCrossSystemEvidence({workspace,secrets});for(const check of cross.checks)upsert(checks,check);topology=cross.map;workspace.stackMap=topology}catch{upsert(checks,{id:'map.cross-system',label:'Cross-system map',status:'WARN',detail:'Provider health checks completed, but the relationship map could not be fully evaluated in this run.',evidence:{source:'weaverelay-cross-system'}})}try{const environment=await buildEnvironmentDeploymentEvidence({workspace,secrets});for(const check of environment.checks)upsert(checks,check)}catch{upsert(checks,{id:'env.deployment-truth',label:'Environment / deployment truth',status:'WARN',detail:'The live provider checks completed, but environment/deployment metadata could not be fully evaluated.',evidence:{source:'weaverelay-environment-deployment'}})}try{const runtimePayments=await buildRuntimePaymentsEvidence({workspace,secrets});for(const check of runtimePayments.checks)upsert(checks,check)}catch{upsert(checks,{id:'runtime.payments-truth',label:'Runtime / payment boundary',status:'WARN',detail:'The live provider checks completed, but Railway runtime or Stripe webhook metadata could not be fully evaluated.',evidence:{source:'weaverelay-runtime-payments'}})}
if(secrets.runpod){try{const runpod=await buildRunPodComfyUIEvidence({runpodToken:secrets.runpod});for(const check of runpod.checks)upsert(checks,check);const relationship=await buildRunPodAppRelationshipEvidence({workspace,runpodInventory:runpod.inventory});for(const check of relationship.checks)upsert(checks,check);const comfy=relationship.checks.find(x=>x.id==='map.app-runpod-comfyui')||runpod.checks.find(x=>x.id==='map.runpod-comfyui');workspace.providers=(workspace.providers||[]).map(p=>p.id==='comfyui'?{...p,status:comfy?.status==='PASS'?'connected':comfy?.status==='FAIL'?'error':'detected',detail:comfy?.detail||p.detail,checkedAt:now}:p)}catch{upsert(checks,{id:'map.runpod-comfyui',label:'RunPod → ComfyUI',status:'WARN',detail:'RunPod is connected, but the read-only ComfyUI relationship probe could not complete in this diagnosis.',evidence:{source:'weaverelay-runpod-comfyui',computeStartedForTest:false,responseBodiesRetained:false}})}}
if(workspace.lastRepair?.type==='railway-supabase-url'&&workspace.lastRepair?.configurationVerified===true){try{const runtime=await verifyRailwayRepairRuntime({workspace,railwayToken:secrets.railway});if(runtime){upsert(checks,{id:'repair.railway-runtime',label:'Repaired Railway runtime',status:runtime.status,detail:runtime.detail,evidence:{source:'weaverelay-repair-verification',...(runtime.evidence||{})}});if(runtime.evidence?.runtimeVerified===true)workspace.lastRepair={...workspace.lastRepair,runtimeVerified:true,runtimeVerifiedAt:now}}}catch{upsert(checks,{id:'repair.railway-runtime',label:'Repaired Railway runtime',status:'WARN',detail:'The configuration repair is saved, but runtime verification could not complete in this diagnosis.',evidence:{source:'weaverelay-repair-verification',runtimeVerified:false}})}try{const dependency=await verifyBackendSupabaseDependency({workspace});if(dependency){upsert(checks,{id:'repair.railway-supabase-dependency',label:'Repaired backend → Supabase',status:dependency.status,detail:dependency.detail,evidence:{source:'weaverelay-backend-dependency-proof',...(dependency.evidence||{})}});if(dependency.evidence?.dependencyVerified===true)workspace.lastRepair={...workspace.lastRepair,dependencyVerified:true,fullChainVerified:true,verifiedAt:now}}}catch{upsert(checks,{id:'repair.railway-supabase-dependency',label:'Repaired backend → Supabase',status:'WARN',detail:'The Railway backend is running, but application-level Supabase verification could not complete in this diagnosis.',evidence:{source:'weaverelay-backend-dependency-proof',dependencyVerified:false}})}}
if(workspace.lastRepair?.type==='stripe-webhook-host'&&workspace.lastRepair?.configurationVerified===true){const boundary=checkById(checks,'map.railway-stripe-webhook'),boundaryVerified=boundary?.status==='PASS';upsert(checks,{id:'repair.stripe-webhook',label:'Repaired Stripe → Railway webhook',status:boundaryVerified?'PASS':'WARN',detail:boundaryVerified?'The saved Stripe webhook points to the Railway service proven to belong to this application. Delivery still requires separate post-repair evidence.':'The Stripe webhook configuration was saved, but the live Stripe → Railway destination relationship is not yet proven by the current diagnosis.',evidence:{source:'weaverelay-stripe-webhook-repair',configurationVerified:true,boundaryVerified,deliveryVerified:false,endpointUrlsRetained:false,signingSecretsRetained:false}});if(workspace.lastRepair?.handlerRepair?.type==='stripe-handler-secret'&&workspace.lastRepair.handlerRepair.configurationVerified===true){try{const runtime=await verifyRailwayRepairRuntime({workspace,railwayToken:secrets.railway});if(runtime){upsert(checks,{id:'repair.stripe-handler-runtime',label:'Repaired Stripe handler runtime',status:runtime.status,detail:runtime.detail,evidence:{source:'weaverelay-repair-verification',...(runtime.evidence||{})}});if(runtime.evidence?.runtimeVerified===true)workspace.lastRepair={...workspace.lastRepair,handlerRepair:{...workspace.lastRepair.handlerRepair,runtimeVerified:true,runtimeVerifiedAt:now}}}}catch{upsert(checks,{id:'repair.stripe-handler-runtime',label:'Repaired Stripe handler runtime',status:'WARN',detail:'The Stripe handler configuration is saved, but Railway runtime verification could not complete.',evidence:{source:'weaverelay-repair-verification',runtimeVerified:false}})}}let delivery=null;try{delivery=await verifyStripeWebhookDelivery({workspace,stripeToken:secrets.stripe});if(delivery){upsert(checks,{id:'repair.stripe-webhook-delivery',label:'Stripe webhook delivery → Railway handler',status:delivery.status,detail:delivery.detail,evidence:{source:'weaverelay-stripe-delivery-proof',...(delivery.evidence||{})}});if(delivery.evidence?.deliveryVerified===true)workspace.lastRepair={...workspace.lastRepair,runtimeVerified:true,deliveryVerified:true,fullChainVerified:true,verifiedAt:now};else workspace.lastRepair={...workspace.lastRepair,deliveryVerified:false,fullChainVerified:false}}}catch{upsert(checks,{id:'repair.stripe-webhook-delivery',label:'Stripe webhook delivery → Railway handler',status:'WARN',detail:'The Stripe webhook URL is correct, but delivery verification could not complete in this diagnosis.',evidence:{source:'weaverelay-stripe-delivery-proof',deliveryVerified:false,eventPayloadsRetained:false,endpointUrlsRetained:false}})}if(delivery?.status==='FAIL')try{const handler=await diagnoseStripeHandlerFailure({workspace,stripeToken:secrets.stripe});if(handler)upsert(checks,{id:'repair.stripe-handler-failure',label:'Stripe webhook handler failure',status:handler.status,detail:handler.detail,evidence:{source:'weaverelay-stripe-handler-diagnosis',classification:handler.classification,...(handler.evidence||{})}})}catch{upsert(checks,{id:'repair.stripe-handler-failure',label:'Stripe webhook handler failure',status:'WARN',detail:'Stripe delivery is failing after the URL repair, but the handler failure class could not be safely isolated in this run.',evidence:{source:'weaverelay-stripe-handler-diagnosis',handlerFailureIsolated:false,syntheticStripeEventSent:false}})}}
const snapshot=sanitizeSnapshot({...seed,product:workspace.name,generatedAt:now,topology,checks}),diagnosis=diagnoseSnapshot(snapshot),stripeBoundary=checkById(snapshot.checks,'map.railway-stripe-webhook');sharpenStripeHandlerFinding(diagnosis,snapshot);addHandlerRedeployAction(diagnosis,snapshot);if(stripeBoundary?.status==='WARN'&&secrets.railway&&secrets.stripe)try{const proposal=await inspectStripeWebhookRepair({workspace,railwayToken:secrets.railway,stripeToken:secrets.stripe});if(proposal.eligible){const f=diagnosis.findings?.find(x=>x.id==='railway-stripe-webhook-unproven');if(f){f.explanation='Stripe has one enabled webhook with a webhook-like route, and WeaveRelay proved one different Railway production host for this app. A host-only repair is available; the existing path and query string will be preserved.';f.actions=['Approve a host-only Stripe webhook destination correction. WeaveRelay will re-read the endpoint immediately before writing and verify the saved host afterward.'];f.repair={supported:true,approvalRequired:true,type:'stripe-webhook-host',provider:'stripe',label:'FIX STRIPE WEBHOOK'}}}}catch{}workspace.diagnosis=diagnosis;workspace.lastDiagnosticSnapshot=snapshot;workspace.updatedAt=now;workspace.status=diagnosis.status==='healthy'?'ready':'needs_action';await writeWorkspace(workspace);return json(200,{ok:true,workspaceId:workspace.id,diagnosis,checks:snapshot.checks,stackMap:topology})}catch(error){return safeError(error)}};
