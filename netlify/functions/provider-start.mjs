import crypto from'node:crypto';import{requireUser}from'./_auth.mjs';import{requireWorkspace}from'./_workspace-store.mjs';import{PROVIDERS}from'./_provider-catalog.mjs';import{issueOAuthState}from'./_oauth.mjs';import{json,safeError}from'./_http.mjs';
const challenge=v=>crypto.createHash('sha256').update(v).digest('base64url');
export default async request=>{if(request.method!=='POST')return json(405,{error:'Method not allowed.'});try{const user=await requireUser(request),body=await request.json(),provider=String(body.provider||''),workspace=await requireWorkspace(user.id,body.workspaceId);if(!PROVIDERS[provider]||!(workspace.providers||[]).some(p=>p.id===provider))return json(400,{error:'Provider is not part of this workspace.'});if(provider==='railway'){const clientId=process.env.CONNECT_RAILWAY_CLIENT_ID;if(!clientId||!process.env.CONNECT_RAILWAY_CLIENT_SECRET)return json(409,{error:'Railway read-only authorization is not configured on WeaveRelay yet.',configuration:'CONNECT_RAILWAY_CLIENT_ID + CONNECT_RAILWAY_CLIENT_SECRET'});const codeVerifier=crypto.randomBytes(32).toString('base64url'),{state,redirectUri}=await issueOAuthState({request,userId:user.id,workspaceId:workspace.id,provider,metadata:{pkceVerifier:codeVerifier}}),u=new URL('https://backboard.railway.com/oauth/auth');u.searchParams.set('response_type','code');u.searchParams.set('client_id',clientId);u.searchParams.set('redirect_uri',redirectUri);u.searchParams.set('state',state);u.searchParams.set('scope','openid profile offline_access project:viewer');u.searchParams.set('prompt','consent');u.searchParams.set('code_challenge',challenge(codeVerifier));u.searchParams.set('code_challenge_method','S256');return json(200,{ok:true,provider,authorizationUrl:u.toString(),access:'read-only project selection'});}const{state,redirectUri}=await issueOAuthState({request,userId:user.id,workspaceId:workspace.id,provider});if(provider==='github'){const clientId=process.env.CONNECT_GITHUB_CLIENT_ID;if(!clientId)return json(409,{error:'GitHub authorization is not configured on WeaveRelay yet.',configuration:'CONNECT_GITHUB_CLIENT_ID + CONNECT_GITHUB_CLIENT_SECRET'});const scope=process.env.CONNECT_GITHUB_SCOPE||'read:user repo';const u=new URL('https://github.com/login/oauth/authorize');u.searchParams.set('client_id',clientId);u.searchParams.set('redirect_uri',redirectUri);u.searchParams.set('state',state);u.searchParams.set('scope',scope);return json(200,{ok:true,provider,authorizationUrl:u.toString()});}if(provider==='netlify'){const clientId=process.env.CONNECT_NETLIFY_CLIENT_ID;if(!clientId)return json(409,{error:'Netlify authorization needs one-time WeaveRelay OAuth app setup before customers can connect.',configuration:'CONNECT_NETLIFY_CLIENT_ID'});const u=new URL('https://app.netlify.com/authorize');u.searchParams.set('client_id',clientId);u.searchParams.set('response_type','token');u.searchParams.set('redirect_uri',redirectUri);u.searchParams.set('state',state);return json(200,{ok:true,provider,authorizationUrl:u.toString(),access:'Netlify OAuth — no personal access token copying'});}const envPrefix={supabase:'CONNECT_SUPABASE',stripe:'CONNECT_STRIPE'}[provider];return json(409,{error:`${PROVIDERS[provider].label} adapter is structurally ready but its live OAuth exchange is not activated in this build.`,configuration:envPrefix?`${envPrefix}_CLIENT_ID + ${envPrefix}_CLIENT_SECRET`:'Provider-specific least-privilege authorization required.'});}catch(error){return safeError(error)}};
