import test from'node:test';import assert from'node:assert/strict';import{proveRepairOutcome,guardFixedLabel}from'../netlify/functions/_post-repair-proof.mjs';
const now=Date.parse('2026-09-03T19:20:00Z'),repair={completedAt:'2026-09-03T19:15:00Z'};
test('configuration success alone cannot become FIXED',()=>{const p=proveRepairOutcome({repair,now,checks:[{id:'repair.config.verify',status:'PASS',evidence:{source:'weaverelay-repair-verification',observedAt:'2026-09-03T19:16:00Z'}}]});assert.equal(p.fixed,false);assert.equal(p.state,'VERIFYING');assert.equal(guardFixedLabel('FIXED',p),'VERIFYING')});
test('pre-repair functional proof cannot prove repair',()=>{const p=proveRepairOutcome({repair,now,checks:[{id:'browser.synthetic',status:'PASS',evidence:{source:'weaverelay-synthetic-browser',observedAt:'2026-09-03T19:14:00Z'}}]});assert.equal(p.fixed,false)});
test('fresh post-repair independent functional proof can become FIXED',()=>{const p=proveRepairOutcome({repair,now,checks:[{id:'customer.runtime.delivery',status:'PASS',evidence:{source:'weaverelay-runtime-payments',observedAt:'2026-09-03T19:18:00Z'}}]});assert.equal(p.fixed,true);assert.equal(p.state,'FIXED');assert.equal(p.proof.checkId,'customer.runtime.delivery')});
test('stale proof cannot become FIXED',()=>{const p=proveRepairOutcome({repair:{completedAt:'2026-09-03T18:00:00Z'},now,checks:[{id:'browser.synthetic',status:'PASS',evidence:{source:'weaverelay-synthetic-browser',observedAt:'2026-09-03T18:01:00Z'}}]});assert.equal(p.fixed,false)});
