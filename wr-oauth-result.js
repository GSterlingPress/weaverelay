(()=>{const q=new URLSearchParams(location.search),provider=q.get('oauth'),error=q.get('error'),connected=q.get('connected');if(!provider||(!error&&!connected))return;const clean=v=>String(v||'').replace(/[\u0000-\u001f\u007f]/g,' ').replace(/\s+/g,' ').trim().slice(0,240),label=provider==='github'?'GitHub':clean(provider);if(error){const detail=clean(error)||'The provider authorization callback failed.';alert(`${label} connection failed: ${detail}`);return}if(connected==='1')alert(`${label} connected successfully.`);})();
