(()=>{
const providers=()=>document.querySelector('#providers');
const DOMAIN_PROVIDER=[['.up.railway.app','railway'],['.netlify.app','netlify'],['.vercel.app','vercel'],['.onrender.com','render'],['.pages.dev','cloudflare'],['.supabase.co','supabase']];
function domainProvider(){const raw=document.querySelector('#siteOrigin')?.textContent?.trim();if(!raw)return null;try{const host=new URL(raw).hostname.toLowerCase();const hit=DOMAIN_PROVIDER.find(([suffix])=>host.endsWith(suffix));return hit?.[1]||null}catch{return null}}
function ensure(){const grid=providers();if(!grid)return;let details=document.querySelector('#availableConnections');if(!details){details=document.createElement('details');details.id='availableConnections';details.style.marginTop='18px';details.innerHTML='<summary style="cursor:pointer;font-weight:800;letter-spacing:.03em">AVAILABLE CONNECTIONS <span style="opacity:.65;font-weight:600">— not part of the current connected stack</span></summary><div id="availableProviders" class="providers" style="margin-top:14px"></div>';grid.insertAdjacentElement('afterend',details)}return details}
let last='';
function split(){const grid=providers(),details=ensure();if(!grid||!details)return;const available=details.querySelector('#availableProviders'),all=[...grid.querySelectorAll(':scope > .provider-card'),...available.querySelectorAll(':scope > .provider-card')];if(!all.length)return;
  const byProvider=new Map();for(const card of all){const id=card.dataset.provider||'';if(!id)continue;const chip=(card.querySelector('.chip')?.textContent||'').trim().toLowerCase(),connected=chip.startsWith('connected'),existing=byProvider.get(id);if(!existing||connected&&!existing.connected){if(existing)existing.card.remove();byProvider.set(id,{card,connected})}else card.remove()}
  const related=domainProvider();const sig=[...byProvider].map(([id,v])=>`${id}:${(v.card.querySelector('.chip')?.textContent||'').trim()}:${id===related?'domain':''}`).sort().join('|');if(sig===last)return;last=sig;
  for(const [id,{card,connected}] of byProvider){const isRelated=id===related&&!connected;card.classList.toggle('domain-related',isRelated);card.dataset.domainEvidence=isRelated?'provider-owned-hostname':'';if(isRelated){const chip=card.querySelector('.chip');if(chip){chip.textContent='DETECTED';chip.classList.remove('bad','good');chip.classList.add('warn')}const p=card.querySelector('p');if(p)p.textContent='Detected from the production website hostname. Connect this provider to verify it.'}(connected||isRelated?grid:available).appendChild(card)}
  const uniqueAvailable=new Set([...available.querySelectorAll(':scope > .provider-card')].map(c=>c.dataset.provider).filter(Boolean));const count=uniqueAvailable.size;details.querySelector('summary').innerHTML=`AVAILABLE CONNECTIONS <span style="opacity:.65;font-weight:600">— ${count} supported provider${count===1?'':'s'} not currently connected</span>`;details.hidden=count===0;}
setInterval(split,900);setTimeout(split,250);setTimeout(split,1200);window.addEventListener('popstate',()=>{last='';setTimeout(split,250)});document.addEventListener('click',e=>{if(e.target?.id==='diagnose'||e.target?.classList?.contains('workspace-link')){last='';setTimeout(split,700)}});
})();
